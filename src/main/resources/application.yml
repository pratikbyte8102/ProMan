spring:
  application:
    name: proman
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://dpg-d82afbkvikkc73ehtsf0-a.ohio-postgres.render.com/proman_prod}
    username: ${DATABASE_USERNAME:pratik94313}
    password: ${DATABASE_PASSWORD:hbgq6GsYjdFQpFxBapWLKXbsE2niNlSX}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:my-256-bit-secret-key-for-development-only-change-in-prod}
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

server:
  port: ${PORT:8080}


springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
